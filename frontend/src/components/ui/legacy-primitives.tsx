/** Compatibility shim only — feature code should import from @/shared/ui/primitives. */
export {
  Badge,
  Button,
  ButtonLink,
  Card,
  EmptyState,
  ErrorState,
  Input,
  PageHeader,
  Progress,
  Select,
  Skeleton,
  Textarea,
} from "@/shared/ui/primitives";
